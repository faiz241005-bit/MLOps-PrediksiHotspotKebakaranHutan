# =============================================================================
# FireGuard MLOps — Dependencies
# Pinned versions untuk reproducibility (lockfile-style).
# Update via: pip install -U <package> && pip freeze > requirements.txt
# =============================================================================

# --- Core data & ML ----------------------------------------------------------
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2
lightgbm==4.3.0
scipy==1.13.0

# --- Geospatial (untuk bbox per provinsi) ------------------------------------
geopandas==0.14.4
shapely==2.0.4

# --- Data fetching & APIs ----------------------------------------------------
requests==2.32.3
httpx==0.27.0
tenacity==8.3.0  # retry dengan exponential backoff untuk API call

# --- Configuration & validation ----------------------------------------------
pyyaml==6.0.1
pydantic==2.7.1
python-dotenv==1.0.1  # load .env (lokal dev only, JANGAN di-commit .env)

# --- Experiment tracking & versioning ----------------------------------------
mlflow==2.13.0
dvc[s3]==3.50.1  # DVC dengan S3 remote (alternatif: dvc[gdrive])

# --- Parquet I/O (data/processed) --------------------------------------------
pyarrow==15.0.2

# --- Visualization & EDA -----------------------------------------------------
matplotlib==3.8.4
seaborn==0.13.2
plotly==5.22.0

# --- Notebook & Jupyter ------------------------------------------------------
jupyter==1.0.0
ipykernel==6.29.4
nbstripout==0.7.1  # strip notebook output sebelum commit (security + cleanliness)

# --- Serving (planned LK selanjutnya) ----------------------------------------
fastapi==0.111.0
uvicorn[standard]==0.30.1

# --- Testing & quality -------------------------------------------------------
pytest==8.2.1
pytest-cov==5.0.0
pytest-mock==3.14.0
hypothesis==6.103.0  # property-based testing

# --- Linting & formatting ----------------------------------------------------
black==24.4.2
flake8==7.0.0
isort==5.13.2
mypy==1.10.0
pre-commit==3.7.1

# --- Drift detection ---------------------------------------------------------
# Library lightweight untuk PSI; bisa diganti evidently jika ingin lebih kaya
# evidently==0.4.25
